import styled from "styled-components"

export const CHHeader = styled.header`
  background: url(${(props) => props.theme.headerBackgroundURL}) no-repeat;
  height: 197px;
  box-sizing: border-box;
  display: flex;
  padding: 25px;
`
